@extends('[[ layout ]]')

@section('content')
<div class="px-4 sm:px-6 lg:px-8">
    <div class="md:flex md:items-center md:justify-between mb-6">
        <h1 class="text-xl font-semibold text-gray-900">Edit [[ class ]]</h1>
        <a href="{{ route('[[ routeName ]].index') }}"
           class="text-sm text-gray-600 hover:text-gray-900">&larr; Back</a>
    </div>

    <div class="bg-white shadow rounded-lg max-w-2xl">
        <div class="px-6 py-5">
            @if ($errors->any())
                <div class="rounded-md bg-red-50 p-4 mb-6">
                    <ul class="list-disc pl-5 space-y-1">
                        @foreach ($errors->all() as $error)
                            <li class="text-sm text-red-700">{{ $error }}</li>
                        @endforeach
                    </ul>
                </div>
            @endif

            <form method="POST" action="{{ route('[[ routeName ]].update', $[[ variable ]]) }}" novalidate>
                @csrf
                @method('PUT')
[[ formFields ]]
                <div class="flex items-center gap-3 mt-6">
                    <button type="submit"
                            class="inline-flex justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                        Update [[ class ]]
                    </button>
                    <a href="{{ route('[[ routeName ]].index') }}"
                       class="text-sm text-gray-600 hover:text-gray-900">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>
@endsection
