@extends('[[ layout ]]')

@section('content')
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-12 col-xl-8">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="card-title mb-0">[[ class ]] Details</h5>
                    <div class="d-flex gap-2">
                        <a href="{{ route('[[ routeName ]].edit', $[[ variable ]]) }}" class="btn btn-outline-primary btn-sm">
                            <i class="mdi mdi-pencil"></i> Edit
                        </a>
                        <a href="{{ route('[[ routeName ]].index') }}" class="btn btn-outline-secondary btn-sm">
                            <i class="mdi mdi-arrow-left"></i> Back
                        </a>
                    </div>
                </div>

                <div class="card-body">
[[ showFields ]]
                    <hr>

                    <form method="POST" action="{{ route('[[ routeName ]].destroy', $[[ variable ]]) }}"
                          onsubmit="return confirm('Are you sure you want to delete this record?')">
                        @csrf
                        @method('DELETE')
                        <button type="submit" class="btn btn-outline-danger btn-sm">
                            <i class="mdi mdi-trash-can"></i> Delete [[ class ]]
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
